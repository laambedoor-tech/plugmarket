# Netlify configuration for Plug Market
[build]
  publish = "."
  functions = "netlify/functions"

# Map /api/* to Netlify Functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
